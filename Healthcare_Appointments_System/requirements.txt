# ─────────────────────────────────────────────
# Healthcare Appointments System — Backend
# Python package requirements (pip install -r requirements.txt)
# Tested with Python 3.13
# ─────────────────────────────────────────────

# ── Web Framework ──────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0          # ASGI server (includes websockets + watchfiles)

# ── Database ───────────────────────────────────
sqlalchemy==2.0.40                 # Async ORM (SQLAlchemy 2)
asyncpg==0.30.0                    # Async PostgreSQL driver for SQLAlchemy
alembic==1.15.2                    # DB migrations

# ── Authentication & Security ──────────────────
python-jose[cryptography]==3.3.0   # JWT encode / decode
passlib[bcrypt]==1.7.4             # Password hashing (bcrypt)
python-multipart==0.0.20           # Required for OAuth2 form-based login

# ── Settings / Config ──────────────────────────
pydantic==2.11.3                   # Data validation (bundled with FastAPI)
pydantic-settings==2.9.1           # .env file loading via BaseSettings

# ── Email Notifications ────────────────────────
fastapi-mail==1.4.2                # Async email helper for FastAPI
aiosmtplib==3.0.2                  # Async SMTP transport (used by fastapi-mail)
jinja2==3.1.6                      # Email HTML templates

# ── HTTP Client (for async testing / external calls) ──
httpx==0.28.1                      # Async HTTP client (also used by TestClient)

# ── Testing ────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.26.0             # Async test support
pytest-cov==6.1.0                  # Coverage reports
aiosqlite==0.20.0                  # Async SQLite driver (in-memory DB for tests)

# ── Development / Utilities ────────────────────
python-dotenv==1.1.0               # Load .env in local dev (fallback)
email-validator==2.2.0             # Pydantic email field validation
